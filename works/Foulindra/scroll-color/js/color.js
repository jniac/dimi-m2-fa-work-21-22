const colors = [
  '#fc0',
  'red',
  'blue',
  





]




const getRandomColor = () => {
  const colors = ['#fc0', 'red', 'blue']
  const index = Math.floor(Math.random() * colors.length)
  return colors[index]
}